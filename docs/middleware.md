# AutoFlow AI - Middleware Stack

> Generated by the **Middleware Generator** from `metadata/middleware/*.yaml`.

This document describes the metadata-driven middleware stack. The stack is
registered on the FastAPI application by `register_middleware()` in
`backend/app/middleware/manager.py`, in the order defined by each
middleware's `order` field (lower order executes first).

## Stack Overview

| Order | Middleware | Kind | Module | Purpose |
|-------|-----------|------|--------|---------|
| 10 | `request_id` | base | `app.middleware.request_id` | Assign and propagate a unique request id on every request. |
| 20 | `correlation_id` | base | `app.middleware.correlation_id` | Propagate and generate correlation ids for distributed tracing. |
| 30 | `health` | base | `app.middleware.health` | Short-circuit health check paths before heavier middleware. |
| 40 | `exception` | base | `app.middleware.exception` | Global exception handler returning consistent JSON errors. |
| 50 | `timing` | base | `app.middleware.timing` | Measure request duration and expose the X-Response-Time header. |
| 60 | `logging` | base | `app.middleware.logging` | Structured request logging via the stdlib logging module. |
| 70 | `metrics` | base | `app.middleware.metrics` | In-memory request metrics (counters and latency histograms). |
| 80 | `rate_limit` | base | `app.middleware.rate_limit` | Fixed-window rate limiting keyed by client ip and path. |
| 90 | `authentication` | base | `app.middleware.authentication` | Validate JWT bearer tokens and populate request.user state. |
| 100 | `authorization` | base | `app.middleware.authorization` | Enforce scope/role requirements recorded on request state. |
| 110 | `tenant` | base | `app.middleware.tenant` | Resolve organization context from headers/token and attach to request state. |
| 120 | `audit` | base | `app.middleware.audit` | Collect audit events for mutating requests. |
| 130 | `security_headers` | base | `app.middleware.security_headers` | Apply security hardening response headers. |
| 140 | `cors` | starlette | `app.middleware.cors` | Cross-origin resource sharing from settings-driven origins. |
| 150 | `compression` | starlette | `app.middleware.compression` | GZip response compression. |

## Execution Order

Requests flow through the stack outermost-first:

```
request_id -> correlation_id -> health -> exception -> timing -> logging -> metrics -> rate_limit -> authentication -> authorization -> tenant -> audit -> security_headers -> cors -> compression
```

## Registering the Stack

```python
from app.middleware.manager import register_middleware

app = FastAPI()
register_middleware(app)
```

Per-middleware options can be overridden at runtime for tests or
environment-specific tuning:

```python
register_middleware(app, overrides={"rate_limit": {"requests_per_minute": 30}})
```

## Middleware Reference

| Middleware | Options |
|-----------|---------|
| `request_id` | `header_name` |
| `correlation_id` | `header_name` |
| `health` | `paths` |
| `exception` | `expose_details` |
| `timing` | `header_name` |
| `logging` | `log_headers` |
| `metrics` | `snapshot_enabled` |
| `rate_limit` | `requests_per_minute`, `exempt_paths` |
| `authentication` | `auto_error`, `algorithm` |
| `authorization` | `default_deny`, `public_paths` |
| `tenant` | `header_name` |
| `audit` | `log_audit` |
| `security_headers` | `content_security_policy`, `frame_options`, `nosniff`, `hsts_max_age` |
| `cors` | `allow_credentials`, `allow_methods`, `allow_headers` |
| `compression` | `minimum_size` |

## Disabling Middleware

Set `enabled: false` for a middleware in `metadata/middleware/*.yaml`
and regenerate. The module remains generated but is excluded from the
registration order.

## Validation

1. `python -m ast` parsing of every generated module (AST check).
2. Import check of `app.middleware.*` with `PYTHONPATH=backend`.
3. FastAPI startup through `register_middleware(app)`.
4. Registration-order check against `execution_order()`.
5. `pytest tests/middleware -q` integration suite.
