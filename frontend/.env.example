# AutoFlow AI frontend - example environment
# Copy to .env.local and adjust for your deployment.
#
# The single source of truth for the backend base URL. The API client
# (src/lib/api/client.ts) falls back to http://localhost:8000/api/v1
# when this is unset.
NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
