# Repository configuration for Template
entity: Template

searchable_fields: ["name", "description", "category"]
filterable_fields: ["category", "is_public"]
sortable_fields: ["name", "created_at", "usage_count"]
unique_fields: ["slug"]
cache_policy: high
cache_ttl: 600
default_ordering: "-usage_count"
